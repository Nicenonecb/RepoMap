{
  "name": "@repomap/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "repomap": "dist/index.js"
  },
  "scripts": {
    "predev": "npm --workspace @repomap/core run build",
    "dev": "tsx src/index.ts",
    "prebuild": "npm --workspace @repomap/core run build",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@repomap/core": "workspace:*",
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
