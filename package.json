{
  "name": "repomap-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @repo-map/repomap dev",
    "build": "pnpm --filter @repo-map/repomap build"
  }
}
